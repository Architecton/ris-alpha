from distutils.core import setup

setup(
    version='0.0.0',
    packages=['color_classification', 'checkpoint_generation', 'classification', 'cylinder_recognition', 'digit_recognition', 'qr_recognition', 'sound', 'reconfig', 'detection_objective_approach'],
    package_dir={'': 'scripts'}
)
